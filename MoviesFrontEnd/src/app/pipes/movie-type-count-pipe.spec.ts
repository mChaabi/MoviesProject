import { MovieTypeCountPipe } from './movie-type-count-pipe';

describe('MovieTypeCountPipe', () => {
  it('create an instance', () => {
    const pipe = new MovieTypeCountPipe();
    expect(pipe).toBeTruthy();
  });
});
